<main>
    <div class="container-fluid">

        <div class="row mb-3">
            <div class="col-3">
                <a href="/create-types" class="btn btn-outline-success">Create a new region</a>
            </div>
        </div>
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
            {{#if hasTypes}}
                {{#each types as |Type|}}
                    <div class="col">
                        <div class="card shadow-sm">
                            <div class="card-body">
                                <h3>{{Type.name}}</h3>
                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="">
                                        <a href="/edit-types/{{Type.id}}?edit=true" class="btn btn-sm btn-outline-primary">Edit</a>
                                        <form class="form-inline" id="form-type" method="POST" action="/delete-types">
                                            <input type="hidden" name="TypesId" value="{{Type.id}}">
                                            <button type="submit" id="delete-type" class="btn btn-sm btn-outline-danger">Delete</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div> 
                {{/each}}
            {{else}}
                <h2>NO TYPES ADDED</h2>
            {{/if}}
        </div>
    </div>
</main>